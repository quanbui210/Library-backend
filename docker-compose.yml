services:
  db:
    container_name: librarydb_b43b
    image: postgres
    environment:
      POSTGRES_USER: quanbui
      POSTGRES_PASSWORD: wjL3pusDMNnh1KXq2HchAUC9wBqX8Wmb
      PGDATA: /data/postgres
    volumes:
      - db:/data/postgres
    ports:
      - "5432:5432"
    networks:
      - db
    restart: unless-stopped

networks:
  db:
    driver: bridge
volumes:
  db:
